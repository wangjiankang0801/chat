{
  "name": "ai-companion",
  "private": true,
  "version": "0.1.0",
  "description": "类猫箱的 AI 陪伴网页版：BYOK 接入各大模型 API，人设注入你的聊天记录",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "npm install --include=dev && npm --prefix server install --include=dev && npm --prefix web install --include=dev",
    "dev": "concurrently -k -n web,server -c cyan,yellow \"npm --prefix web run dev\" \"npm --prefix server run dev\"",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}